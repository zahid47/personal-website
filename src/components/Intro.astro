---
import { getLangFromUrl, useTranslations } from "../i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<article>
  <p class="font-pally-medium mt-1.5 text-sm==md text-white">
    {t("hero.yahallo")} 👋
  </p>
  <h1 class="font-pally-medium font-medium text-2xl text-white sm:text-3xl">
    {t("hero.nice-to-meet-you")}
  </h1>
  <p class="font-pally-regular mt-2 text-sm==md text-white mb-4">
    {t("hero.intro")}
  </p>
</article>
